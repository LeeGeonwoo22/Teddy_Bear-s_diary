import 'package:flutter/material.dart';

class Toteddy extends StatelessWidget {
  const Toteddy({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
